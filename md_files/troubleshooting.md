# 🔧 Решение проблем TechStats

## Проблема: Кнопка не активируется после анализа

### ✅ ИСПРАВЛЕНО в текущей версии

**Что было:**
- После завершения анализа кнопка оставалась неактивной
- Нужно было обновлять страницу

**Что изменилось:**
- Кнопка автоматически активируется при получении `stage: 'finished'`
- Кнопка активируется при ошибках
- WebSocket корректно закрывается после завершения

**Проверка:**
1. Запустите анализ
2. Дождитесь завершения (100%)
3. Кнопка "Начать анализ" должна быть активной
4. Можно сразу запустить новый анализ

---

## Проблема: Прогресс не обновляется

### ✅ ИСПРАВЛЕНО в текущей версии

**Что было:**
- Показывались пустые значения "Найдено-, Страниц-, Реальных запросов0"
- Прогресс-бар стоял на 0%

**Что изменилось:**
- Добавлена инициализация с пустыми значениями по умолчанию
- Сервер отправляет все необходимые поля в каждом сообщении
- Прогресс обновляется на каждом этапе

**Проверка:**
1. Запустите анализ
2. Вы должны видеть:
   - "Получаем список вакансий..." (0%)
   - "Найдено 850 вакансий на 9 страницах" (10%)
   - "Загружено страниц: 3/9" (20-50%)
   - "Обработано вакансий: 200/850" (50-95%)
   - "Анализ завершен!" (100%)

---

## Проблема: WebSocket не подключается

### Симптомы:
```
WebSocket connection to 'ws://localhost:8000/ws/analyze' failed
ERR_UNKNOWN_URL_SCHEME
```

### Причины и решения:

#### 1. Браузер пытается открыть ws:// как веб-страницу

**НЕ открывайте** `ws://localhost:8000/ws/analyze` в адресной строке браузера!

**Правильно:**
- Откройте `index.html` в браузере
- JavaScript автоматически подключится к WebSocket

#### 2. Сервер не запущен

**Проверка:**
```bash
# Должно выдать информацию о сервере
curl http://localhost:8000/health
```

**Если ошибка:**
```bash
# Запустите сервер
python main.py
```

#### 3. Неправильный порт

**В `index.html` проверьте:**
```javascript
const WS_URL = 'ws://localhost:8000/ws/analyze';
```

**В консоли сервера должно быть:**
```
Uvicorn running on http://0.0.0.0:8000
```

Если сервер на другом порту, измените URL.

#### 4. Проблемы с CORS / файловым протоколом

**Если открываете через `file://`:**

Запустите простой HTTP сервер:
```bash
# В папке с index.html
python -m http.server 8080
```

Затем откройте: http://localhost:8080/index.html

---

## Проблема: Соединение закрывается сразу

### В логах сервера:
```
INFO:     connection open
INFO:     connection closed
```

### Причины:

#### 1. JavaScript ошибка при отправке данных

**Откройте консоль браузера (F12)** и проверьте ошибки.

**Типичные ошибки:**
- `Uncaught TypeError: Cannot read property...`
- `Invalid JSON`

#### 2. Сервер не получает данные

**Добавьте логирование в консоль:**
```javascript
// В startAnalysis() перед websocket.send()
console.log('Отправляем данные:', JSON.stringify(formData));
```

#### 3. Версия библиотек

**Проверьте версии:**
```bash
pip list | grep -E "fastapi|uvicorn|websockets"
```

**Должно быть:**
```
fastapi      0.104.1
uvicorn      0.24.0
websockets   12.0
```

**Обновление:**
```bash
pip install --upgrade -r requirements.txt
```

---

## Проблема: Ошибка "Module not found"

### Симптом:
```
ModuleNotFoundError: No module named 'websockets'
```

### Решение:

#### 1. Не активировано виртуальное окружение

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

**Проверка:** В начале строки должно быть `(venv)`

#### 2. Не установлены зависимости

```bash
pip install -r requirements.txt
```

#### 3. Запущен не тот Python

```bash
# Проверьте какой Python используется
which python  # Linux/Mac
where python  # Windows

# Должен быть из venv
```

---

## Проблема: Медленный анализ

### Первый запрос медленный - это НОРМАЛЬНО

**Ожидаемое время:**
- 100 вакансий: ~30 секунд
- 300 вакансий: ~1-2 минуты
- 850 вакансий: ~2-3 минуты

### Второй запрос должен быть быстрым

**Если второй запрос такой же медленный:**

#### 1. Проверьте кэш
```bash
curl http://localhost:8000/cache/stats
```

**Должно быть:**
```json
{
  "cache_size": 850,  // > 0 !
  "cache_hit_rate": 98.5
}
```

#### 2. Используете ли одну и ту же вакансию?

**Кэш работает только для ОДИНАКОВЫХ вакансий:**

✅ Правильно:
```
Запрос 1: "System Analyst" + "UML"
Запрос 2: "System Analyst" + "BPMN"  // та же вакансия!
```

❌ Неправильно:
```
Запрос 1: "System Analyst" + "UML"
Запрос 2: "Data Engineer" + "Python"  // другая вакансия!
```

#### 3. Прошло ли > 24 часов?

Кэш устаревает через 24 часа. Очистите и начните заново:
```bash
curl -X DELETE http://localhost:8000/cache/clear
```

---

## Проблема: Высокая нагрузка на CPU

### Причина: Слишком много параллельных потоков

**В `main.py` уменьшите:**
```python
MAX_WORKERS = 3  # было 5
```

### Причина: Слишком большой запрос

**Уменьшите количество страниц:**
- В интерфейсе: поле "Макс. страниц" → 3-5
- Или в коде: `max_pages: int = Field(default=3, ...)`

---

## Проблема: Rate Limit от HH.ru

### Симптом:
```
429 Too Many Requests
```

### Решение:

**В `main.py` уменьшите скорость:**
```python
MAX_REQUESTS_PER_SECOND = 5  # было 7
REQUEST_DELAY = 0.2  # было 0.1
```

---

## Проблема: Кэш не сохраняется между сессиями

### Это НОРМАЛЬНО

Кэш хранится в памяти (RAM) и сбрасывается при перезапуске сервера.

**Если нужен постоянный кэш:**
1. Используйте Redis
2. Или сохраняйте в файлы/БД

---

## Диагностика через консоль браузера

### Откройте консоль (F12)

**Что смотреть:**

#### 1. Network tab
- Есть ли запрос к `ws://localhost:8000/ws/analyze`?
- Статус: 101 Switching Protocols (правильно)
- Статус: 404 / 500 (ошибка)

#### 2. Console tab
- JavaScript ошибки
- `console.log()` сообщения

#### 3. WebSocket frames
- Messages tab в Network
- Видны ли сообщения от сервера?

---

## Полезные команды для диагностики

```bash
# Проверить работает ли сервер
curl http://localhost:8000/health

# Проверить кэш
curl http://localhost:8000/cache/stats

# Очистить кэш
curl -X DELETE http://localhost:8000/cache/clear

# Проверить документацию API
# Откройте: http://localhost:8000/docs

# Просмотр логов в реальном времени (Linux/Mac)
tail -f nohup.out

# Запуск с подробными логами
uvicorn main:app --reload --log-level debug
```

---

## Контрольный чек-лист

Перед запросом помощи проверьте:

- [ ] Сервер запущен (`python main.py`)
- [ ] Виртуальное окружение активировано
- [ ] Установлены все зависимости (`pip install -r requirements.txt`)
- [ ] index.html открыт через HTTP, не file://
- [ ] Браузер современный (Chrome, Firefox, Edge)
- [ ] Консоль браузера не показывает ошибок (F12)
- [ ] Порт 8000 не занят другим приложением
- [ ] API доступен: http://localhost:8000/health возвращает 200

---

## Всё еще не работает?

### Создайте минимальный тест:

**test_websocket.html:**
```html
<!DOCTYPE html>
<html>
<body>
    <h1>WebSocket Test</h1>
    <div id="status">Connecting...</div>
    <script>
        const ws = new WebSocket('ws://localhost:8000/ws/analyze');
        
        ws.onopen = () => {
            document.getElementById('status').textContent = 'Connected! ✅';
            ws.send(JSON.stringify({
                vacancy_title: 'Test',
                technology: 'Python',
                exact_search: true,
                area: 113,
                max_pages: 1
            }));
        };
        
        ws.onmessage = (event) => {
            console.log('Получено:', event.data);
        };
        
        ws.onerror = (error) => {
            document.getElementById('status').textContent = 'Error! ❌';
            console.error('WebSocket error:', error);
        };
    </script>
</body>
</html>
```

Если этот тест не работает - проблема в настройке сервера/окружения.
Если работает - проблема в основном index.html.

---

**Нужна дополнительная помощь?** Создайте issue с полными логами сервера и консоли браузера.
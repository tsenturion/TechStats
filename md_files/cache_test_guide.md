# 🧪 Как проверить, что кэш работает

## Быстрый тест

### Шаг 1: Первый запрос
1. Откройте `index.html`
2. Введите:
   - Вакансия: `System Analyst`
   - Технология: `UML`
3. Нажмите "Начать анализ"
4. **Следите за прогрессом:**
   - В разделе "Из кэша" должно быть **0** или очень мало
   - В разделе "Реальных запросов" будет расти число
   - Попадание в кэш будет **0-5%**

### Шаг 2: Второй запрос (сразу после первого)
1. **НЕ ПЕРЕЗАГРУЖАЙТЕ СТРАНИЦУ**
2. Измените только технологию на: `BPMN`
3. Нажмите "Начать анализ"
4. **Обратите внимание:**
   - Загрузка вакансий будет **быстрее** (те же вакансии)
   - В разделе "Из кэша" будет **большое число** (800+)
   - Попадание в кэш будет **90-99%** 🎯
   - Анализ пройдет в **5-10 раз быстрее**!

---

## Детальный тест с объяснениями

### Что происходит внутри

#### Первый запрос: `System Analyst` + `UML`

**Этап 1: Загрузка списка вакансий**
```
Найдено 850 вакансий на 9 страницах
Реальных запросов: 9 (по одному на каждую страницу)
Из кэша: 0
```

**Этап 2: Анализ каждой вакансии**
```
Для каждой из 850 вакансий:
1. Проверка названия (быстро)
2. Проверка сниппета (быстро)
3. Загрузка полного описания с API (медленно!)
   → Сохранение в кэш с временной меткой
   
Итого:
Реальных запросов: 9 + 850 = 859
Из кэша: 0
Попадание в кэш: 0%
Время: ~2-3 минуты
```

#### Второй запрос: `System Analyst` + `BPMN`

**Этап 1: Загрузка списка вакансий**
```
Найдено 850 вакансий на 9 страницах
Реальных запросов: 9 (список вакансий не кэшируется)
Из кэша: 0

❓ Почему список не кэшируется?
→ Список вакансий может измениться (новые вакансии)
→ Но! Если вакансия есть в кэше, её описание не загружается
```

**Этап 2: Анализ каждой вакансии**
```
Для каждой из 850 вакансий:
1. Проверка названия (быстро)
2. Проверка сниппета (быстро)
3. Загрузка описания:
   → Проверка: есть ли в кэше?
   → Проверка: не устарело ли? (< 24 часов)
   → Если ДА → берем из кэша (мгновенно!) ✅
   → Если НЕТ → загружаем с API
   
Итого:
Реальных запросов: 9 + 0-5 = 9-14 (только новые вакансии!)
Из кэша: 845-850
Попадание в кэш: 98-99% 🎯
Время: ~10-20 секунд (в 10 раз быстрее!)
```

---

## Визуальные индикаторы

### В процессе анализа

**Первый запрос (холодный кэш):**
```
┌─────────────────────────────────┐
│ Обработано: 400/850             │
│ Найдено с технологией: 320      │
│ Реальных запросов: 409          │
│ Из кэша: 0                      │
│ Попадание в кэш: 🎯 0%          │ ← Оранжевый индикатор
└─────────────────────────────────┘
```

**Второй запрос (горячий кэш):**
```
┌─────────────────────────────────┐
│ Обработано: 400/850             │
│ Найдено с технологией: 280      │
│ Реальных запросов: 9            │
│ Из кэша: 400                    │
│ Попадание в кэш: 🎯 97.8%       │ ← Зеленый индикатор
└─────────────────────────────────┘
```

### В финальных результатах

**Статистика запросов (второй запрос):**
```
┌─────────────────┐
│       9         │ ← Маленькое число!
│ API запросов    │
│ 845 из кэша     │ ← Большое число!
│ (98.9%)         │
└─────────────────┘
```

---

## Что делать если кэш не работает

### Проблема 1: Кэш всегда 0%

**Причина:** Перезагружаете страницу между запросами
**Решение:** НЕ перезагружайте! Кэш хранится на сервере, но счетчики сбрасываются

### Проблема 2: Кэш работает, но процент низкий

**Возможные причины:**
1. Появились новые вакансии (норма)
2. Прошло > 24 часов (устаревшие записи удалены)
3. Вы очистили кэш через `/cache/clear`

**Проверка через API:**
```bash
curl http://localhost:8000/cache/stats
```

Смотрите на `cache_size` - должно быть > 0

### Проблема 3: Второй запрос всё равно медленный

**Причина:** Используете разные названия вакансий
**Пример:**
- Первый запрос: `Data Engineer` → 850 вакансий (ID: 1-850)
- Второй запрос: `Python Developer` → 1200 вакансий (ID: 2000-3200)
- **Результат:** Новые вакансии, кэш не поможет!

**Решение:** Используйте то же название вакансии, меняйте только технологию

---

## Полезные команды

### Проверить статистику кэша
```bash
curl http://localhost:8000/cache/stats
```

### Очистить кэш
```bash
curl -X DELETE http://localhost:8000/cache/clear
```

### Проверить здоровье системы
```bash
curl http://localhost:8000/health
```

---

## Идеальный сценарий тестирования

```
1. Запустите сервер
2. Откройте index.html
3. Первый запрос:
   - Вакансия: System Analyst
   - Технология: UML
   - Результат: Кэш 0-5%, время ~2 мин
   
4. Второй запрос (сразу):
   - Вакансия: System Analyst (ТО ЖЕ!)
   - Технология: BPMN (ДРУГОЕ!)
   - Результат: Кэш 98%+, время ~20 сек ✅
   
5. Третий запрос (сразу):
   - Вакансия: System Analyst (ТО ЖЕ!)
   - Технология: SQL (ДРУГОЕ!)
   - Результат: Кэш 98%+, время ~20 сек ✅
```

---

## FAQ

**Q: Почему кэш не 100%?**
A: Потому что список вакансий всегда загружается заново (могут появиться новые). Только описания вакансий кэшируются.

**Q: Сколько хранится кэш?**
A: 24 часа по умолчанию. Можно изменить в `CACHE_TTL_HOURS`.

**Q: Можно ли сделать кэш постоянным?**
A: Сейчас кэш в памяти (пропадает при перезапуске). Для постоянного кэша нужна база данных.

**Q: Влияет ли регион на кэш?**
A: Нет. Кэш по ID вакансии, независимо от региона.

**Q: Что если вакансия обновилась на HH.ru?**
A: Кэш устареет через 24 часа и обновится автоматически.